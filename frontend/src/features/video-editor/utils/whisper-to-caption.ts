/** Re-exported from the shared composition package (see store/types.ts). */
export { whisperToCaptions } from '@sohizi/video-composition'

export type {
  OpenAiToCaptionsOutput,
  WhisperCaptionsInput,
} from '@sohizi/video-composition'
