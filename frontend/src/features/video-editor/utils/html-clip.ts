/** Re-exported from the shared composition package (see store/types.ts). */
export {
  applyLiveHtmlConfig,
  buildDefaultValues,
  buildVariableOverride,
  extractVariableSchema,
  getLiveConfigApplyOptions,
  injectGsap,
  injectVariables,
  prepareHtmlDocument,
} from '@sohizi/video-composition'

export type { LiveConfigApplyOptions } from '@sohizi/video-composition'
